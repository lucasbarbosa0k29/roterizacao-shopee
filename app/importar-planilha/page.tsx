// app/importar-planilha/page.tsx
import { redirect } from "next/navigation";

export default function ImportarPlanilhaPage() {
  // Se você já tem outra página real de upload, troque aqui.
  // Por enquanto, vamos mandar pro / (depois você muda).
  redirect("/");
}